kind: pipeline
type: docker
name: default

steps:
- name: install
  image: node:lts-alpine3.15
  commands:
      - node -v
      - npm -v
      - yarn --version
      - yarn config set cache-folder .yarn-cache
      - yarn install --pure-lockfile
- name: scp
  image: appleboy/drone-scp
  host: 34.85.60.220
  username: hanesumi
  secrets: [ ssh_password ]
  target: /Users/hanesumi/Desktop/Drone/tttita.test
  source:
    - index.html
